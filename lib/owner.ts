// The true owner's email allowlist. Shared by the dashboard's access control
// (app/dashboard/page.tsx) and server routes that need to email the owner (e.g.
// app/api/tasks/on-complete), so there's one place to change it.
export const OWNER_EMAILS = ["mukhiadil2009@gmail.com"]
